{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "▶ Run Game",
      "type": "cppdbg",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "cwd": "${workspaceFolder}",
      "externalConsole": true,
      "preLaunchTask": "CMake: build",
      "stopAtEntry": false,
      "MIMode": "gdb"
    },
    {
      "name": "🐛 Debug Game",
      "type": "cppdbg",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "cwd": "${workspaceFolder}",
      "externalConsole": true,
      "preLaunchTask": "CMake: build",
      "stopAtEntry": false,
      "MIMode": "gdb"
    }
  ]
}